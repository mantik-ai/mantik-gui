type: object
description: Represents a untrained model definition
properties:
    modelRepositoryId:
        type: string
    description:
        type: string
    git:
        $ref: ./CodeRepository.yaml
    commit:
        type: string
    labels:
        type: array
        items:
            $ref: ./Label.yaml
required:
    - modelRepositoryId
