type: object
description: Represents a data repository
properties:
    dataRepositoryId:
        type: string
        format: uuid
    dataRepositoryName:
        type: string
    uri:
        type: string
    commitHash:
        type: string
    description:
        type: string
    labels:
        type: array
        items:
            $ref: ./Label.yaml
required:
    - dataRepositoryId
    - uri
