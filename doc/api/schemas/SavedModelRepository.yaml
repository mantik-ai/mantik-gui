type: object
description: Represents a trained model
properties:
    trainedModelRepositoryId:
        type: string
    modelRepository:
        $ref: ./ModelRepository.yaml
    uri:
        type: string

required:
    - trainedModelRepositoryId
    - uri
